# Deliberately small. Every import here is a version that can shift under us on Kaggle,
# so anything implementable in ~50 lines is implemented instead of imported:
#   jiwer    -> src/metrics.py    (hand-rolled Levenshtein)
#   pydantic -> src/config.py     (dataclasses)
#   datasets -> scripts/          (parquet read via pyarrow; datasets 5.0 dropped scripts)

pyyaml
numpy
scipy              # resample_poly: dataset is 24 kHz, Whisper needs 16 kHz
soundfile
pyarrow            # VIVOS refs/convert/parquet branch
huggingface_hub
torch
# Pinned 2026-09-10 on the H200 box: 5.17.0 forwards **kwargs from
# WhisperForConditionalGeneration.forward down into WhisperDecoder, where peft's
# SEQ_2_SEQ_LM wrapper's own input_ids=None collides with decoder_input_ids
# ("got multiple values for keyword argument 'input_ids'") and training dies on step 1.
# 5.0.0 is what the v3-r16 / v4-mixed-r16 runs used.
transformers==5.0.0
peft
accelerate         # transitive via Trainer, pinned here so it is visible

# Deliberately UNPINNED, and deliberately not hand-rolled: YouTube changes server-side
# often enough that an old yt-dlp simply stops working, and extraction is not a ~50-line
# job. scripts/probe_youtube_captions.py and scripts/fetch_youtube.py only. The latter
# also needs `ffmpeg` on PATH (system dep, not pip)
# to produce 16 kHz mono wav -- fetch_youtube.py raises naming it when absent.
yt-dlp

# Dev
pytest
matplotlib         # scripts/plot_training_curve.py only, not used by src/
